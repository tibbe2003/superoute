// ignore_for_file: file_names

import 'package:flutter/material.dart';

import 'scan_product_widget.dart';

class ProductScanTable extends StatelessWidget {
  const ProductScanTable({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const ScanProductWidget();
  }
}
